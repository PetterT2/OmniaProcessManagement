import { types } from 'typestyle';

export const LinksBlockStyles = {} as {
    blockPadding?: types.NestedCSSProperties;
    linkItemTitle?: types.NestedCSSProperties;
    linkItemTitleText?: types.NestedCSSProperties;
    subHeader?: types.NestedCSSProperties;
    linkItemHover?: types.NestedCSSProperties;
}